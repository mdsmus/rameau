* Rameau 1.0

Esse release envolve o trabalho de 7 meses e contém os seguintes
recursos:

 * estrutura básica do programa, que envolve:
   - analisador léxico e sintático para o formato usado pelo lilypond
   - arquivos de teste (regressão, exemplos, e unidade)
   - biblioteca para lidar com elementos musicais (musiclib)
 * análise harmônica usando o algoritmo do pardo
 * programa binário para testes, conversão e comparação de resultados
 * formato de cifras "pop" para geração de gabaritos
 * arquivo lilypond de 371 corais, sendo que a maioria deles foram "limpos"
