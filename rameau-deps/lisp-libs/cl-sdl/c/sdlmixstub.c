#include <stdio.h>
#include <SDL.h>
#include <SDL_endian.h>	/* Used for the endian-dependent 24 bpp mode */
#include <SDL_mixer.h>

/*
int play_channel (int channel, Mix_Chunk *chunk, int loops) {
  return Mix_PlayChannel (channel, chunk, loops);
}

Mix_Chunk *load_wav (char *file) {
  return Mix_LoadWAV (file);
}
*/
